'use strict';

import React, { Component } from 'react';

const ListItems = React.createClass({
    render() {
        return (
            <section>Kind of index route</section>
        );
    }
});

export default ListItems;